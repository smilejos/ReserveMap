# ReserveMap Agent 維護指南

本專案是唯讀靜態地圖。網站介面不編輯資料；所有正式收藏只存在於 `data/definitions.json` 與 `data/places.json`。修改資料前，先完整閱讀 `REQUIREMENTS.md` 及本文件。

## 不可違反的規則

- 不得將密碼、訂房憑證、私人聯絡資料或其他敏感資訊寫入 JSON。
- 地點的 `uid` 建立後永久不變；移動地區、重新分類、排序或重新編號均不得更換。
- 已刪除地點的 UID 不得重複使用。產生新 UID 時使用標準 UUID。
- `id` 是可變的目前識別碼，格式為 `{countryId}-{categoryId}-{五位流水號}`。
- 一般新增時，流水號取同一「國家＋大類別」現有最大值加一，不填補缺號。
- 只有使用者明確要求「全面整理／重新編號」時才可重排 ID。
- 備註是純文字，可保留換行；不要加入 HTML。
- 不要把正式地點資料寫入程式碼、測試或 `localStorage`。
- 每次資料異動後必須執行 `npm run validate:data`；失敗時不得交付或部署。

## 新增地點

1. 從使用者提供的 Google Maps 長網址或短網址取得最終網址。
2. 解析並核實名稱、座標及可取得的地址。常見長網址可能在 `@緯度,經度` 或查詢參數中包含座標。
3. 短網址可安全跟隨 HTTPS redirect，但不得使用 Google Places API。若頁面或網址無法可靠取得座標，要求使用者補充名稱或座標，不可猜造。
4. 依使用者內容確認國家、地區、大類別與小類別。可合理推測；有多個合理選項時才詢問。
5. 新地區或新小類別必須先加入 `definitions.json`，再新增地點。
6. 建立前檢查：
   - 完全相同或正規化後相同的 Google Maps URL。
   - 同一地區內相同或相似的名稱。
   - 同一地區內距離非常接近的座標。
7. 發現疑似重複時，列出目前 ID、名稱、UID、距離或相同欄位，等待使用者判斷；不得直接新增。
8. 產生未曾使用的 UUID，並取得該國家＋大類別下一個五位流水號。
9. `rating` 預設為 `null`；只有使用者明確提供時才設定為 1–5、每次 0.5。
10. `address` 無法取得時設為 `null`；`note` 未提供時設為空字串。
11. 寫入後執行完整驗證，回覆目前 ID、名稱、地區、分類及驗證結果。

## Google Maps 網址

使用者提供的網址必須使用 HTTPS，且為以下形式之一：

- `maps.app.goo.gl`
- `goo.gl/maps/...`
- `google.com/maps/...`、`google.com.tw/maps/...`、`google.co.jp/maps/...`，可含 `www` 或 `maps` 子網域

`maps.app.goo.gl` 與 `goo.gl/maps/...` 只作為輸入，不得直接寫入 `places.json`。收到短網址時必須自行正規化：

1. 安全跟隨 HTTPS redirect，取得 Google Maps 最終長網址；不得使用 Google Places API。
2. 核實最終網址顯示的名稱與座標和要新增或修改的地點一致。
3. 若最終網址的 `/data=...` 段含有 `!1s0x...:0x...!`，擷取 `!1s` 後、下一個 `!` 前符合 `0x[十六進位]:0x[十六進位]` 的完整值作為 `ftid`。
4. 使用標準 URL 編碼組成 `https://www.google.com/maps?q={地點名稱}&ftid={ftid}`；`q` 與 `ftid` 都必須透過 URL API 或等效方法編碼，不得手動拼接未編碼文字。
5. 若最終網址已是含 `query_place_id` 或 `ftid` 的 Google Maps 長網址，保留該精確地點識別網址，不要改成只有座標的搜尋網址。
6. 若無法取得 `ftid`，保存已核實的最終 Google Maps 長網址；不得退回保存短網址，也不得猜造識別值。
7. 寫入前再次開啟正規化後的網址，確認仍指向同一地點；重複檢查一律使用正規化後的網址。

若 Google 提供的新官方網域未在驗證白名單中，先確認所有權及安全性，再同步更新 `scripts/validate-data.mjs` 與相關測試；不要為了通過驗證放寬成任意網域。

## 修改地點

1. 通常以目前 ID 尋找地點；修改前回讀並核對名稱與 UID。
2. 只修改使用者要求的欄位，UID 永遠不變。
3. 國家或大類別改變時，依新群組最大流水號產生新 ID。
4. 地區必須屬於指定國家，小類別必須屬於指定大類別。
5. 完成後驗證並回覆變更摘要；ID 改變時明列「舊 ID → 新 ID」。

## 刪除地點

1. 以目前 ID 或足以唯一辨識的名稱尋找，並核對名稱與 UID。
2. 多筆候選時先詢問，不可自行選擇。
3. 從 `places` 陣列移除，不建立封存欄位或墓碑資料。
4. 驗證後回覆被刪除的 ID、名稱及 UID，提醒可由 Git 歷史復原。

## 全面整理與重新編號

- 只在使用者明確要求時執行。
- 未指定排序時，依國家、地區、大類別、小類別在 `definitions.json` 的陣列順序，再依名稱排序。
- 重新編號可重用流水號，但 UID、名稱、座標、URL、地址、備註與評分必須跟著原地點保留。
- 完成後執行驗證，回覆異動數量與舊 ID → 新 ID 對照。

## 地區與分類定義

- 陣列順序就是介面順序，不另加 `sortOrder`。
- 新地區需要唯一 ID、有效國家、顯示名稱、中心座標與 1–22 的縮放值。
- 新小類別需要全域唯一 ID、名稱，以及 `src/config/iconKeys.json` 支援的圖示鍵值。
- 若需要全新圖示鍵值，必須同時更新圖示白名單與 `src/config/icons.tsx` 的 Lucide 元件對照。
- 顏色或圖示調整只改定義，不逐筆改地點。
- 定義異動後確認所有既有參照仍有效。

## 完成檢查

資料維護至少執行：

```bash
npm run validate:data
```

若同時修改程式、schema 或驗證器，執行：

```bash
npm run check
```

驗證器的「警告」代表疑似重複，需人工判斷；「錯誤」必須修正。不要刪除合法警告邏輯來讓資料通過。
